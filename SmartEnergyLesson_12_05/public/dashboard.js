navEl();function navEl(){document.getElementById('nav').innerHTML=nav()} guard(); async function load(){const d=await api('/api/dashboard');price.textContent=d.price+' €/kWh';source.textContent='Источник: '+d.source;vacation.textContent=d.settings.vacation_mode?'Активен':'Выключен';warn.innerHTML=d.warning?`<p class="warn">${d.warning}</p>`:'';devices.innerHTML=d.devices.map(x=>`<div class="item"><h3>${x.name}</h3><p>${x.description||''}</p><p>Лимит: ${x.price_limit}</p><p>Подключение: ${x.connection_type} / ${x.connection_status}</p><p><span class="badge ${x.status}">${x.status}</span> ${x.manual_override?'ручной режим':'авто'}</p><button onclick="toggle(${x.id})">Вкл/выкл вручную</button><button class="ok" onclick="auto(${x.id})">Вернуть авто</button></div>`).join('')||'<p>Нет устройств</p>';const f=await api('/api/forecast');forecast.innerHTML='<table><tr><th>Час</th><th>Цена</th><th>План</th></tr>'+f.map(h=>`<tr><td>${h.hour}:00</td><td>${h.price}</td><td>${h.actions.map(a=>a.device+': '+a.planned_status).join('<br>')}</td></tr>`).join('')+'</table>'}async function toggle(id){await api('/api/devices/'+id+'/toggle',{method:'POST'});load()}async function auto(id){await api('/api/devices/'+id+'/auto',{method:'POST'});load()}load();setInterval(load,15000)
